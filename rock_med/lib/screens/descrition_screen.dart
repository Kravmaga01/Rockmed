import 'package:flutter/widgets.dart';

class DescrtionScreen extends StatelessWidget {
  const DescrtionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Hola');
  }
}
